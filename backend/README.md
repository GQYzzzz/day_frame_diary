# Backend（占位）

计划：**Python + FastAPI**。

## 将来放置内容

- 路由：上传凭证、作品 CRUD、触发生成、查询任务状态
- 存储：与 R2 / S3 / Supabase Storage 对接（预签名上传等）
- 数据库：PostgreSQL 模型与迁移（用户、作品、任务）
- AI 编排：读取 `../prompts`，调用 Vision / LLM，校验 JSON 输出
- 可选：异步任务队列、限流与密钥管理（环境变量）

## 本目录当前状态

尚未初始化虚拟环境与依赖；确认后再添加 `pyproject.toml` 或 `requirements.txt`。
