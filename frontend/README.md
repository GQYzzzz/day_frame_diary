# Frontend（占位）

计划：**Next.js + TypeScript**。

## 将来放置内容

- `app/` 或 `src/app/`：路由与四页流程（首页、上传、结果、历史）
- 模板：按「风格」区分的 React 组件，统一接收文案与图片 URL
- 导出：基于 DOM 的长图 / 多卡导出（html-to-image 等）
- 对外 API 客户端：与 `backend` 的 REST 契约

## 本目录当前状态

尚未初始化 Next.js 项目；确认技术细节后再执行 `create-next-app` 等脚手架命令。
